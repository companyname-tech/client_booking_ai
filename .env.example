# Use mock data adapters (default: true). Set to false when wiring your backend.
VITE_USE_MOCK_DATA=true

# Backend API base URL (used when VITE_USE_MOCK_DATA=false)
VITE_API_URL=http://localhost:3000/api
